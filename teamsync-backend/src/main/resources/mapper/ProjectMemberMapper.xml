<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.teamsync.mapper.ProjectMemberMapper">
    <resultMap id="memberMap" type="ProjectMember">
        <id column="id" property="id"/><result column="project_id" property="projectId"/><result column="user_id" property="userId"/><result column="role" property="role"/><result column="join_time" property="joinTime"/><result column="username" property="username"/><result column="real_name" property="realName"/>
    </resultMap>
    <select id="findByProjectId" resultMap="memberMap">SELECT pm.*, u.username, u.real_name FROM project_member pm LEFT JOIN user u ON pm.user_id=u.id WHERE pm.project_id=#{projectId}</select>
    <select id="findByProjectAndUser" resultMap="memberMap">SELECT * FROM project_member WHERE project_id=#{projectId} AND user_id=#{userId}</select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">INSERT INTO project_member(project_id,user_id,role) VALUES(#{projectId},#{userId},#{role})</insert>
    <delete id="deleteByProjectAndUser">DELETE FROM project_member WHERE project_id=#{projectId} AND user_id=#{userId}</delete>
    <select id="findUserIdsByProjectId" resultType="java.lang.Long">SELECT user_id FROM project_member WHERE project_id=#{projectId}</select>
</mapper>
