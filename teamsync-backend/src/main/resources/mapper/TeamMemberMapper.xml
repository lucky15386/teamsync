<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.teamsync.mapper.TeamMemberMapper">
    <resultMap id="teamMemberMap" type="TeamMember">
        <id column="id" property="id"/>
        <result column="team_id" property="teamId"/>
        <result column="user_id" property="userId"/>
        <result column="role" property="role"/>
        <result column="join_time" property="joinTime"/>
        <result column="username" property="username"/>
        <result column="real_name" property="realName"/>
        <result column="avatar" property="avatar"/>
    </resultMap>
    <select id="findByTeamId" resultMap="teamMemberMap">SELECT tm.*, u.username, u.real_name, u.avatar FROM team_member tm LEFT JOIN user u ON tm.user_id=u.id WHERE tm.team_id=#{teamId}</select>
    <select id="findByUserId" resultMap="teamMemberMap">SELECT * FROM team_member WHERE user_id=#{userId}</select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">INSERT INTO team_member(team_id,user_id,role) VALUES(#{teamId},#{userId},#{role})</insert>
    <delete id="delete">DELETE FROM team_member WHERE team_id=#{teamId} AND user_id=#{userId}</delete>
    <update id="updateRole">UPDATE team_member SET role=#{role} WHERE team_id=#{teamId} AND user_id=#{userId}</update>
</mapper>
