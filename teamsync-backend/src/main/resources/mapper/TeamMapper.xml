<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.teamsync.mapper.TeamMapper">
    <resultMap id="teamMap" type="Team">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="avatar" property="avatar"/>
        <result column="creator_id" property="creatorId"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="creator_name" property="creatorName"/>
        <result column="member_count" property="memberCount"/>
    </resultMap>
    <select id="findById" resultMap="teamMap">SELECT t.*, u.username as creator_name FROM team t LEFT JOIN user u ON t.creator_id=u.id WHERE t.id=#{id}</select>
    <select id="findByUserId" resultMap="teamMap">SELECT t.*, u.username as creator_name, (SELECT COUNT(*) FROM team_member WHERE team_id=t.id) as member_count FROM team t LEFT JOIN user u ON t.creator_id=u.id WHERE t.id IN (SELECT team_id FROM team_member WHERE user_id=#{userId}) ORDER BY t.create_time DESC</select>
    <select id="findAll" resultMap="teamMap">SELECT t.*, u.username as creator_name FROM team t LEFT JOIN user u ON t.creator_id=u.id ORDER BY t.create_time DESC</select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">INSERT INTO team(name,description,avatar,creator_id,status) VALUES(#{name},#{description},#{avatar},#{creatorId},#{status})</insert>
    <update id="update">UPDATE team <set><if test="name!=null">name=#{name},</if><if test="description!=null">description=#{description},</if><if test="avatar!=null">avatar=#{avatar},</if><if test="status!=null">status=#{status},</if></set> WHERE id=#{id}</update>
    <delete id="deleteById">DELETE FROM team WHERE id=#{id}</delete>
    <select id="countMembers" resultType="int">SELECT COUNT(*) FROM team_member WHERE team_id=#{teamId}</select>
</mapper>
